{
  "name": "cypress-automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "chrome-execution": "npx cypress run --browser=chrome --headed --spec \"cypress/e2e/orange-hrm/**/*.js\" --env grepTags=\"smoke",
    "edge-execution": "npx cypress run --browser=edge --headed --spec \"cypress/e2e/orange-hrm/*.js\" --env grepTags=\"smoke",
    "test-execution": "npx cypress run --browser=edge --headed --spec cypress/e2e/orange-hrm/**/*.js"
  },
  "author": "Supraja",
  "license": "ISC",
  "dependencies": {
    "cypress": "^10.3.1"
  },
  "devDependencies": {
    "cypress-cucumber-tagging": "^1.0.3",
    "cypress-grep": "^3.0.3",
    "cypress-testrail-reporter": "^1.3.4",
    "cypress-testrail-simple": "^3.1.0"
  }
}
